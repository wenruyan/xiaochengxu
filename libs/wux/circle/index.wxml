<view class="wux-class {{ classes.wrap }}" style="{{ style }}">
    <canvas style="{{ style }}" canvas-id="circle" />
    <view class="{{ classes.inner }}" style="color:{{ color }}">
    	<slot></slot>
    </view>
</view>